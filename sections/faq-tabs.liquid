{% schema %}
{
  "name": "FAQ Tabs Section",
  "settings": [
    {
      "type": "text",
      "id": "main_title",
      "label": "Main Title",
      "default": "FAQS"
    }
  ],
  "blocks": [
    {
      "type": "tab",
      "name": "Tab",
      "settings": [
        {
          "type": "text",
          "id": "tab_id",
          "label": "Tab ID (unique, e.g. general)",
          "default": "general"
        },
        {
          "type": "text",
          "id": "tab_title",
          "label": "Tab Title",
          "default": "General Questions"
        }
      ]
    },
    {
      "type": "faq",
      "name": "FAQ Item",
      "settings": [
        {
          "type": "text",
          "id": "tab_id",
          "label": "Tab ID (must match the Tab block ID)",
          "default": "general"
        },
        {
          "type": "text",
          "id": "question",
          "label": "Question",
          "default": "Where are you located?"
        },
        {
          "type": "textarea",
          "id": "answer",
          "label": "Answer",
          "default": "We are located in California, USA."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "FAQ Tabs Section",
      "category": "Content"
    }
  ]
}
{% endschema %}

<section class="faq-tabs-section">
  <div class="faq-container">
    <h2 class="faq-title">{{ section.settings.main_title }}</h2>

    {% assign tabs = section.blocks | where: 'type', 'tab' %}
    {% assign faqs = section.blocks | where: 'type', 'faq' %}

    <div class="faq-tabs">
      <ul class="faq-tab-titles">
        {% for tab in tabs %}
          <li class="faq-tab-title {% if forloop.first %}active{% endif %}" data-tab="tab-{{ tab.settings.tab_id }}">
            {{ tab.settings.tab_title }}
          </li>
        {% endfor %}
      </ul>

      <div class="faq-tab-contents">
        {% for tab in tabs %}
          <div class="faq-tab-content {% if forloop.first %}active{% endif %}" id="tab-{{ tab.settings.tab_id }}">
            {% for faq in faqs %}
              {% if faq.settings.tab_id == tab.settings.tab_id %}
                <details>
                  <summary>{{ faq.settings.question }}</summary>
                  <p>{{ faq.settings.answer }}</p>
                </details>
              {% endif %}
            {% endfor %}
          </div>
        {% endfor %}
      </div>
    </div>
  </div>
</section>

<style>
.faq-container {
  text-align: center;
  padding: 40px 20px;
}
.faq-title {
  font-size: 36px;
  font-weight: 700;
  margin-bottom: 30px;
}
.faq-tabs {
  max-width: 1000px;
  margin: 0 auto;
}
.faq-tab-titles {
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;
  list-style: none;
  padding: 0;
  margin: 0 0 20px;
  border-bottom: 3px solid #007dc5;
}
.faq-tab-title {
  flex: 1;
  text-align: center;
  padding: 15px;
  background: #f5f5f5;
  cursor: pointer;
  font-weight: 600;
  border: 1px solid #ddd;
  border-bottom: none;
  transition: all 0.3s ease;
}
.faq-tab-title.active {
  background: linear-gradient(90deg, #21c3f3, #007dc5);
  color: #fff;
}
.faq-tab-content {
  display: none;
  text-align: left;
}
.faq-tab-content.active {
  display: block;
}
details {
  border-bottom: 1px solid #ccc;
  padding: 10px 0;
}
summary {
  cursor: pointer;
  font-weight: 600;
  font-size: 16px;
  position: relative;
}
summary::after {
  content: "▾";
  position: absolute;
  right: 0;
  font-size: 18px;
  transition: transform 0.3s;
}
details[open] summary::after {
  transform: rotate(180deg);
}
details p {
  margin: 10px 0 0;
  font-size: 15px;
  color: #333;
}
@media(max-width: 768px){
  .faq-tab-titles {
    overflow-x: auto;
    white-space: nowrap;
  }
  .faq-tab-title {
    flex: 0 0 auto;
    min-width: 200px;
  }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const tabTitles = document.querySelectorAll('.faq-tab-title');
  const tabContents = document.querySelectorAll('.faq-tab-content');

  tabTitles.forEach(tab => {
    tab.addEventListener('click', () => {
      const target = tab.getAttribute('data-tab');
      tabTitles.forEach(t => t.classList.remove('active'));
      tabContents.forEach(c => c.classList.remove('active'));
      tab.classList.add('active');
      document.getElementById(target).classList.add('active');
    });
  });
});
</script>
