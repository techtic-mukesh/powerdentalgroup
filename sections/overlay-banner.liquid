<section
  class="overlay-banner-wrapper"
  style="background-color: {{ section.settings.background_color }}; color: {{ section.settings.text_color }};"
>
  <div class="overlay-banner-content">
    <h2 style="color: {{ section.settings.text_color }};">{{ section.settings.heading }}</h2>
    {% if section.settings.button_text and section.settings.button_link %}
      <a 
        href="{{ section.settings.button_link }}" 
        class="btn" 
        style="color: {{ section.settings.text_color }}; border-color: {{ section.settings.button_border_color }};"
      >
        {{ section.settings.button_text }}
      </a>
    {% endif %}
  </div>
  <div class="overlay-banner-image">
    {% if section.settings.banner_image %}
      <img src="{{ section.settings.banner_image | img_url: 'master' }}" alt="overlay Image">
    {% endif %}
  </div>
</section>

<style>
.overlay-banner-wrapper {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 60px 40px;
  flex-wrap: wrap;
}
.overlay-banner-content {
  max-width: 40%;
}
.overlay-banner-content h2 {
  font-size: 36px;
  font-weight: 700;
  margin-bottom: 20px;
}
.overlay-banner-content .btn {
  padding: 12px 24px;
  text-decoration: none;
  font-weight: bold;
  border: 2px solid;
  display: inline-block;
  transition: all 0.3s ease;
}
.overlay-banner-content .btn:hover {
  opacity: 0.8;
}
.overlay-banner-image {
  max-width: 55%;
}
.overlay-banner-image img {
  width: 100%;
  height: auto;
  display: block;
}
@media (max-width: 768px) {
  .overlay-banner-wrapper {
    flex-direction: column;
    text-align: center;
  }
  .overlay-banner-content, .overlay-banner-image {
    max-width: 100%;
  }
  .overlay-banner-content h2 {
    font-size: 28px;
  }
}
</style>

{% schema %}
{
  "name": "Overlay Banner",
  "tag": "section",
  "class": "overlay-banner",
  "settings": [
    {
      "type": "image_picker",
      "id": "banner_image",
      "label": "Banner Image"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "YOUR VISION, OUR EXPERTISE."
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text",
      "default": "LET'S COLLABORATE"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color",
      "default": "#00004f"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "button_border_color",
      "label": "Button Border Color",
      "default": "#ffffff"
    }
  ],
  "presets": [
    {
      "name": "Overlay Banner",
      "category": "Custom"
    }
  ]
}
{% endschema %}